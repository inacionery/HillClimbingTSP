plugins {
    id 'java'
    id 'eclipse'
}

sourceCompatibility = '1.8'
targetCompatibility = '1.8'
version = '1.0.0'

repositories { 
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile (name:'minigeo', ext:'jar')
}